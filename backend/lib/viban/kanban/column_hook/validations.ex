defmodule Viban.Kanban.ColumnHook.Validations do
  @moduledoc """
  Validations for the ColumnHook resource.

  This module provides convenient aliasing for column_hook-specific validations.

  Available validations:
  - `Viban.Kanban.ColumnHook.Validations.ValidHookId` - Ensures hook_id is valid
  """

  # The actual validation modules are defined in the validations/ subdirectory.
  # This module exists only for documentation and namespace organization.
end
