defmodule VibanWeb.PageController do
  @moduledoc """
  Placeholder controller - will be replaced by Hologram pages.
  """

  use VibanWeb, :controller

  def index(conn, _params) do
    text(conn, "Hologram setup in progress...")
  end

  def board(conn, _params) do
    text(conn, "Hologram setup in progress...")
  end

  def task(conn, _params) do
    text(conn, "Hologram setup in progress...")
  end
end
